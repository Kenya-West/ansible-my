---
- name: Install and configure Fail2Ban exporter with dependencies
  hosts: analytics_node
  become: false
  become_user: "{{ standard_user }}"

  roles:
    - install_analytics_node_fail2ban_exporter

