---
- name: Install and configure Matrix Synapse server
  hosts: matrix_server
  become: false
  become_user: "{{ standard_user }}"
  roles:
    - name: kwtoolset/resolve_host_relations
      vars:
        resolve_host_relations_group: matrix_server
        resolve_host_relations_component: synapse
    - install_matrix_synapse
