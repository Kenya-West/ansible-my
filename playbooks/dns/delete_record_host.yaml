---
- name: Delete the Ansible-managed DNS records declared by each host
  hosts: domain_management
  gather_facts: false

  roles:
    - role: domain_management
      vars:
        scenarios:
          - cloudflare_delete_records_for_host
