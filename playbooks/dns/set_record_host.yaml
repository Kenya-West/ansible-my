---
- name: Check and set DNS records for the domains
  hosts: domain_management
  gather_facts: false

  roles:
    - role: domain_management
      vars:
        scenarios:
          - cloudflare_check_set_records_for_host
