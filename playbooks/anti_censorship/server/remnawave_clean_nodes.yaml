---
# Deletes every Remnawave node carrying the primary tags of
# vpn_server_remnawave_tags from the panel of each vpn_server_remnawave host,
# after listing them and asking for confirmation. Nodes without those tags are
# left alone.
#
#   -e vpn_server_remnawave_nodes_clean_state=disabled   disable instead of delete
#   -e vpn_server_remnawave_nodes_clean_confirm=true     do not ask
- name: Clean the Ansible-managed Remnawave nodes
  hosts: vpn_server_remnawave
  become: false
  become_user: "{{ standard_user }}"
  gather_facts: false

  tasks:
    - name: Clean the nodes carrying the primary tags
      ansible.builtin.include_role:
        name: configure_remnawave_panel_generate_setup
        tasks_from: clean_nodes
