---
- name: Install DNS by DNSCrypto-proxy
  hosts: general_vps_prepare
  become: true

  roles:
    - name: dns_stack
      vars:
        docker_users:
          - "{{ standard_user }}"
        docker_package_state: present
