---
# The shared secrets of inventory/group_vars/all/z_common_hosts_secrets, the
# same as initial_configure_step2.yaml. See roles/setup/secrets.
#
#   ansible-playbook -i inventory/production.ini playbooks/scenarios/initial_configure_group_vpn.yaml

- name: Run initial configuration tasks
  hosts: localhost
  become: false
  gather_facts: false

  roles:
    - role: setup/secrets
