---
- name: Show backup keys for Autorestic
  hosts: backup_restic_node
  become: false
  become_user: "{{ standard_user }}"

  roles:
    - role: install_backup_node_restic
      vars:
        scenarios:
          - show_keys