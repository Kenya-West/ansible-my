---

- name: Run initial configuration tasks (step 1)
  hosts: localhost
  become: false
  gather_facts: no

  tasks:
    - name: Include initial configuration for inventory
      include_tasks: ansible/bootstrap/create_inventory_for_localhost.yaml
    - name: Include initial configuration users playbook
      include_tasks: ansible/bootstrap/users.yaml

