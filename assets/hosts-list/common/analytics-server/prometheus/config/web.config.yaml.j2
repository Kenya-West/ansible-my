basic_auth_users:
  {% for user, password in prometheus.basic_auth_users.items() %}
  {{ user }}: {{ password }}
  {% endfor %}

# tls_server_config:
#   cert_file: "{{ prometheus.tls_server_config.cert_file }}"
#   key_file: "{{ prometheus.tls_server_config.key_file }}"