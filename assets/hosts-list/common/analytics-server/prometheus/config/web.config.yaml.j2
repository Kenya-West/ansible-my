basic_auth_users:
  {% for user, password in prometheus_server_basic_auth_users.items() %}
  {{ user }}: {{ password }}
  {% endfor %}

# tls_server_config:
#   cert_file: "{{ prometheus_server_tls_cert_file }}"
#   key_file: "{{ prometheus_server_tls_key_file }}"