# This is required, as it otherwise cannot find restic as a command.
PATH="/usr/local/bin:/usr/bin:/bin"

autorestic -c {{ dest_project_fullpath }}/config/.autorestic.yaml --ci cron > /tmp/autorestic.log 2>&1