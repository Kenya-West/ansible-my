#! /usr/bin/env bash

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
. "$SCRIPT_DIR/rclone-pass.sh"

tag=${2:-latest}
autorestic restore -l home_user -c "{{ dest_project_fullpath }}/config/.autorestic.yaml" "$tag" --to ~/restored/specific --include "$1"