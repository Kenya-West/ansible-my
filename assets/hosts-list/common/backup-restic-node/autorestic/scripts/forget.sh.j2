#! /usr/bin/env bash
PATH="/usr/local/bin:/usr/bin:/bin"

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
. "$SCRIPT_DIR/rclone-pass.sh"

autorestic forget -c {{ dest_project_fullpath }}/config/.autorestic.yaml -av