---

services:
  wg:
    image: ghcr.io/wg-easy/wg-easy
    container_name: ${SERVICE_NAME}
    volumes:
      - ./wg-easy:/etc/wireguard
    networks:
      - {{ docker_vpn_network }}
    ports:
      - "${WG_PORT}:51920/udp"
    restart: unless-stopped
    env_file:
      - .env
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1
    labels:
      caddy: "${SERVICE_DOMAIN}:${EXTERNAL_PORT:-443}"
      caddy.import: "logging ${SERVICE_DOMAIN}-wg"
      {% raw %}caddy.0_reverse_proxy: "{{ upstreams ${SERVICE_PORT_INTERNAL} }}"{% endraw %}


networks:
  {{ docker_vpn_network }}:
    external: true

volumes:
  etc_wireguard:
