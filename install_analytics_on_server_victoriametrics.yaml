---
- name: Install and configure victoriametrics with dependencies
  hosts: analytics_server
  become: false
  become_user: "{{ standard_user }}"
  roles:
    - install_analytics_server_victoriametrics