---
# tasks file for roles/install_matrix_synapse
- name: Capture this role's path so nested role can use the parent role_path
  ansible.builtin.set_fact:
    this_role_path: "{{ role_path }}"
  tags:
    - always

- name: Add to source_paths the current role templates directory
  ansible.builtin.set_fact:
    source_paths: >-
      {{
        (
            source_paths_add_role_dirs
            | default([])
            | map('regex_replace', '^(.*)$', this_role_path ~ '/\1/')
            | list
          )
        + (source_paths_add_repo_dirs | default([]))
        + (source_paths_add_user_dirs | default([]))
      }}
  tags:
    - always

- name: Check which source_paths actually exist
  ansible.builtin.stat:
    path: "{{ item }}"
  loop: "{{ source_paths }}"
  delegate_to: localhost
  become: false
  register: source_paths_stat
  tags:
    - always

- name: Exclude non-existing paths from source_paths
  ansible.builtin.set_fact:
    source_paths: >-
      {{
        source_paths_stat.results
        | selectattr('stat.exists')
        | map(attribute='item')
        | list
      }}
  tags:
    - always

- name: Create non-existent parent directories for {{ dest_path_full }}
  ansible.builtin.include_role:
    name: kwtoolset/create_dirs_traversal
    apply:
      tags:
        - update_config_hard
  vars:
    create_dirs_traversal_path: "{{ dest_path_full }}"
    create_dirs_traversal_strip_leaf: false
  tags:
    - update_config_hard

- name: Check if docker-compose.yml exists at {{ dest_path_full }}
  ansible.builtin.stat:
    path: "{{ dest_path_full }}/docker-compose.yml"
  register: matrix_server_synapse_docker_compose_file
  tags:
    - update_image_hard
    - update_config_hard

- name: Ensure Docker network {{ matrix_server_network_name }} exists with specific settings
  community.docker.docker_network:
    name: "{{ matrix_server_network_name }}"
    driver: bridge
    state: present

- name: compose_pull
  ansible.builtin.include_role:
    name: kwtoolset/compose_pull
    apply:
      tags:
        - update_image_hard
        - update_image_soft
  vars:
    project_paths:
      - "{{ dest_path_full }}"
  tags:
    - update_image_hard
    - update_image_soft
  when: matrix_server_synapse_docker_compose_file.stat.exists

- name: compose_down
  ansible.builtin.include_role:
    name: kwtoolset/compose_down
    apply:
      tags:
        - update_image_hard
        - update_config_hard
  vars:
    project_paths:
      - "{{ dest_path_full }}"
  tags:
    - update_image_hard
    - update_config_hard
  when: not matrix_server_synapse_docker_compose_file.stat.exists

- name: copy_files_templating
  ansible.builtin.include_role:
    name: kwtoolset/copy_files_templating
    apply:
      tags:
        - update_config_hard
        - update_config_soft
  vars:
    copy_files_templating_dir_list:
      - src: "{{ source_path }}"
        dest: "{{ dest_path_full }}"
        default_mode: "0775"
    copy_files_templating_exclude_list:
      - "data/"
  with_items: "{{ source_paths }}"
  loop_control:
    loop_var: source_path
  tags:
    - update_config_hard
    - update_config_soft

- name: compose_restart
  ansible.builtin.include_role:
    name: kwtoolset/compose_restart
    apply:
      tags:
        - never
        - update_config_soft
  vars:
    project_paths:
      - "{{ dest_path_full }}"
  tags:
    - never
    - update_config_soft

- name: Check if data directory exists at {{ dest_path_full }}
  ansible.builtin.stat:
    path: "{{ dest_path_full }}/data"
  register: matrix_server_synapse_data_dir
  tags:
    - recreate_config
    - update_config_hard

- name: Remove data directory at {{ dest_path_full }}/data if it exists
  become: true
  become_user: root
  ansible.builtin.file:
    path: "{{ dest_path_full }}/data"
    state: absent
  when: matrix_server_synapse_data_dir.stat.exists
  tags:
    - recreate_config

- name: Check if data directory exists (again) at {{ dest_path_full }}
  ansible.builtin.stat:
    path: "{{ dest_path_full }}/data"
  register: matrix_server_synapse_data_dir
  tags:
    - recreate_config

- name: Create data directory at {{ dest_path_full }}/data if it does not exist
  ansible.builtin.file:
    path: "{{ dest_path_full }}/data"
    state: directory
    mode: "0775"
  when: not matrix_server_synapse_data_dir.stat.exists
  tags:
    - update_config_hard
    - recreate_config

- name: Check if homeserver file exists at {{ dest_path_full }}
  ansible.builtin.stat:
    path: "{{ dest_path_full }}/data/homeserver.yaml"
  register: matrix_server_synapse_config_file
  tags:
    - update_config_hard
    - recreate_config

- name: Since homeserver.yaml does not exist, generate it by running the container once
  ansible.builtin.include_tasks: create_config.yml
  when: not matrix_server_synapse_config_file.stat.exists
  tags:
    - update_config_hard
    - recreate_config

- name: compose_up
  ansible.builtin.include_role:
    name: kwtoolset/compose_up
    apply:
      tags:
        - update_image_hard
        - update_image_soft
        - update_config_hard
  vars:
    project_paths:
      - "{{ dest_path_full }}"
  tags:
    - update_image_hard
    - update_image_soft
    - update_config_hard
    - recreate_config

- name: Add port {{ matrix_server_external_port | default('undefined') }} to UFW
  become: true
  become_user: root
  community.general.ufw:
    rule: allow
    port: "{{ matrix_server_external_port }}"
    proto: tcp
  when:
    - matrix_server_external_port is defined
    - matrix_server_external_port | string | length > 0