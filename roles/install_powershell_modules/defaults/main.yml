---
# defaults file for roles/install_powershell_modules
powershell_profile_modules:
  # full schema is:
  # - name: "posh-git"            # module name on PowerShell Gallery
  #   scope: "CurrentUser"      # Optional. Default is "CurrentUser"
  #   force: true                 # Optional. Default is true
  - name: "PSReadline"
  - name: "PSScriptTools"
  - name: "posh-git"
  - name: "PSFzf"
