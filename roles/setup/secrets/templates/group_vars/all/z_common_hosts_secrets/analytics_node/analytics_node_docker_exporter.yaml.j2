# The prometheus user is added by the role from
# prometheus_server_scrape_credentials.docker_exporter of the analytics server this
# host relates to (host_relations).
docker_exporter_server_users:
  - name: {{ standard_user }}
    password: "{{ initial_configure_generated_password_analytics_node_docker_exporter_standard_user }}"
    hash_scheme: bcrypt
