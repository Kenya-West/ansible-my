#! /usr/bin/env bash

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
. "$SCRIPT_DIR/rclone-pass.sh"

autorestic unlock --force -c {{ dest_path_full }}/config/.autorestic.yaml -v
autorestic exec -c {{ dest_path_full }}/config/.autorestic.yaml -av -- unlock
