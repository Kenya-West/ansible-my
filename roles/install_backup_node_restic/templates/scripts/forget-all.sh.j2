#! /usr/bin/env bash

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"
. "$SCRIPT_DIR/rclone-pass.sh"

autorestic exec -c {{ dest_path_full }}/config/.autorestic.yaml -av -- forget --tag ar:location:home_user --keep-last 0 --unsafe-allow-remove-all