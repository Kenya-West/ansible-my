---
- name: Install Docker
  hosts: general_vps_prepare
  become: true

  roles:
    - name: ./roles/install_docker
      vars:
        docker_users:
          - "{{ standard_user }}"
        docker_package_state: present
